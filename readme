To create a serverless project:
serverless create --template aws-nodejs --path <name of project>

To configure credentials:
serverless config credentials -o --provider aws --key=<ID da chave de acesso> --secret <Chave de acesso secreta>

To deploy:
serverless deploy -v

To invoke:
serveless invoke -f hello(My Function) -l